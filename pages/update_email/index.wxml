<view class="main-flex-container">
  <view class="errorMsg {{show_error ? 'error_show' : 'error_hide'}}">{{error_msg}}</view>
  <swiper class="swiper" current="{{currentStep}}" duration="300">
    <swiper-item catchtouchmove="stopTouchMove">
      <form class="step1-form" bindsubmit="step1FormSubmit" bindreset="formReset">
        <view class="input-flex-container">
          <view class="labelText">验证身份，Step1</view>
          <view class="labelText">修改成功后，原邮箱账户名将无法继续登录或收付款。所有信息保存到新账户中。</view>
          <view class="labelText">原邮箱: {{old_email}}</view>
          <view class="flex-container">
            <view class="input-group">
              <image src="../../images/phone.png"></image>
              <input name="password" password="true" placeholder="支付密码" />
            </view>
          </view>
          <view class="flex-container">
            <button form-type="submit" type="primary" loading="{{show_loading}}" disabled="{{show_loading}}" class="pbtn" size="default">提交申请</button>
          </view>
        </view>
      </form>
    </swiper-item>
    <swiper-item catchtouchmove="stopTouchMove">
      <form class="step2-form" bindsubmit="step2FormSubmit" bindreset="formReset">
        <view class="input-flex-container">
        <view class="labelText">解绑原邮箱，Step2</view>
        <view class="labelText">一封邮件已发送到您的原邮箱，请输入邮件内验证码。</view>
          <view class="flex-container">
            <view class="input-group">
              <image src="../../images/activate.png"></image>
              <input name="code" placeholder="验证码" />
            </view>
          </view>
          <view class="flex-container">
            <button form-type="submit" type="primary" loading="{{show_loading}}" disabled="{{show_loading}}" class="pbtn" size="default">激活</button>
          </view>
        </view>
      </form>
    </swiper-item>
    <swiper-item catchtouchmove="stopTouchMove">
      <form class="step1-form" bindsubmit="step3FormSubmit" bindreset="formReset">
        <view class="input-flex-container">
          <view class="labelText">修改邮箱，Step3</view>
          <view class="flex-container">
            <view class="{{inputStyle}}">
              <image src="../../images/mailbox.png"></image>
              <input name="new_email" bindinput="checkEmail" placeholder="新邮箱地址" />
            </view>
          </view>
          <view class="flex-container">
            <button form-type="submit" type="primary" loading="{{show_loading}}" disabled="{{show_loading}}" class="pbtn" size="default">下一步</button>
          </view>
        </view>
      </form>
    </swiper-item>
    <swiper-item catchtouchmove="stopTouchMove">
      <form class="step2-form" bindsubmit="step4FormSubmit" bindreset="formReset">
        <view class="input-flex-container">
        <view class="labelText">绑定新邮箱，Step4</view>
        <view class="labelText">一封邮件已发送到您的新邮箱，请输入邮件内验证码。</view>
          <view class="flex-container">
            <view class="input-group">
              <image src="../../images/activate.png"></image>
              <input name="code" placeholder="验证码" />
            </view>
          </view>
          <view class="flex-container">
            <button form-type="submit" type="primary" loading="{{show_loading}}" disabled="{{show_loading}}" class="pbtn" size="default">验证</button>
          </view>
        </view>
      </form>
    </swiper-item>
    <swiper-item catchtouchmove="stopTouchMove">
      <view class="register-success" bindreset="formReset">
        <view class="input-flex-container">
          <view class="flex-container">
            <view>绑定成功！</view>
          </view>
          <view class="flex-container">
            <button type="primary" bindtap="clickFinish" class="pbtn" size="default">确认</button>
          </view>
        </view>
      </view>
    </swiper-item>
  </swiper>
</view>